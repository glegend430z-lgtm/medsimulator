[package]
name = "inv_hms_reports_engine"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "inv_hms_reports_engine"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
